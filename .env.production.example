# ============= PRODUCTION ENVIRONMENT CONFIGURATION =============
# This is an example of how your .env should look in production
# Copy this to .env on your production server and update the values

# ============= MULTI-TENANT CONFIGURATION =============
MULTI_TENANCY_MODE="SCHEMA"

# Master Database (tracks all tenants, subscriptions, payments)
# Replace with your production database URL
MASTER_DATABASE_URL="postgresql://username:password@your-db-host:5432/cafemate_master?schema=public"

# PostgreSQL Admin Connection (for creating tenant schemas)
POSTGRES_ADMIN_URL="postgresql://username:password@your-db-host:5432/cafemate_shared"

# Database Host for Tenant Databases
DB_HOST="your-db-host.com"

# Database Encryption Key (32 bytes for AES-256)
# IMPORTANT: Generate a NEW key for production using: openssl rand -base64 32
DB_ENCRYPTION_KEY="GENERATE_NEW_KEY_FOR_PRODUCTION"

# Super Admin Domain
# Your super admin subdomain (e.g., admin.cafemate.com)
SUPER_ADMIN_DOMAIN="admin.yourdomain.com"

# ============= TENANT DATABASE (Production) =============
# Not needed in SCHEMA mode, but keep for compatibility
DATABASE_URL="postgresql://username:password@your-db-host:5432/cafemate_shared?schema=public"

# ============= NEXTAUTH CONFIGURATION =============
# IMPORTANT: Generate a NEW secret for production using: openssl rand -base64 32
NEXTAUTH_SECRET="GENERATE_NEW_SECRET_FOR_PRODUCTION"

# CRITICAL: Set this to your production domain
# NextAuth will use this as the base URL
# For multi-tenant, you can use your main domain or leave it dynamic
NEXTAUTH_URL="https://yourdomain.com"

# ============= APPLICATION CONFIGURATION =============
# Your production domain
NEXT_PUBLIC_APP_URL="https://yourdomain.com"
NEXT_PUBLIC_APP_NAME="Cafe Management System"

# ============= CRON JOB CONFIGURATION =============
# IMPORTANT: Generate a strong secret for production
CRON_SECRET="GENERATE_STRONG_SECRET_FOR_PRODUCTION"

# ============= PRODUCTION NOTES =============
# 1. ALWAYS use HTTPS in production (https://)
# 2. Generate NEW secrets for NEXTAUTH_SECRET, DB_ENCRYPTION_KEY, and CRON_SECRET
# 3. Use strong database passwords
# 4. Configure your DNS for wildcard subdomains: *.yourdomain.com → your-server-ip
# 5. Set up SSL certificates for your domain (e.g., Let's Encrypt with wildcard cert)
# 6. Example tenant URLs in production:
#    - https://hungry.yourdomain.com
#    - https://baithak.yourdomain.com
#    - https://admin.yourdomain.com (super admin)
